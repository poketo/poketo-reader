console.log(process.env.NODE_ENV);

describe('db', () => {
  describe('insertBookmark', () => {
    it('adds a bookmark to the database', () => {});

    it('errors for duplicate bookmark ids', () => {});
  });
});
